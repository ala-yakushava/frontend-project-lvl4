doctype html
html(lang="en")
  head
    title= Title
    script.
      var gon = !{JSON.stringify(gon)};
  body.bg-light
    nav.navbar.navbar-expand-lg.navbar-dark.bg-info
      .container
        a.navbar-brand.text-uppercase.font-weight-bold(href="/") Chat
        button.navbar-toggler(data-toggle="collapse" data-target="#navbarSupportedContent")
          span.navbar-toggler-icon
    .container.mt-5
      #chat
      // block content
    script(src='/assets/main.js')
